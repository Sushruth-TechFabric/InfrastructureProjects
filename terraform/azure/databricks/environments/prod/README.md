# environments/prod — intentional stub

The `.tf` files here are deliberately empty (0-byte placeholders). Prod will be
created from the `environments/staging/` root (the clean verbatim-copy source)
when promotion is decided — do not deploy from this directory.
